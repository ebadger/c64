# Cross-layer check

A spec or system-layer file changed. Verify whether source/codegen, PRG/D64 media,
emulator devices, the WASM bridge, web client, and umbrella spec are still consistent.
Update only the affected layers and their implementation status; do not create a process
report for the check.
