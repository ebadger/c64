# Placeholder values for scripts/dev/instantiate.sh
# Copy to template.config, edit, then run:  scripts/dev/instantiate.sh template.config
# (Lines starting with # are ignored. TEMPLATE_BASE_REF is required unless this is a
# direct clone of the canonical template; the review date defaults to today when blank.)

PROJECT_NAME=My AI Project
CEO=your-github-handle
REPO_SLUG=your-handle/your-repo
DEFAULT_BRANCH=main
STACK=e.g. Python 3.12, FastAPI, Postgres 16
PROD_URL=https://example.com
TEMPLATE_BASE_REF=
TEMPLATE_REVIEW_DATE=
