{
  "name": "c64",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic browser/Node source-to-artifact pipeline for Commodore 64 software (assembler, PRG, D64).",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/",
    "test:pipeline": "node --test tests/",
    "test:wasm": "node --test tests/wasm-smoke.test.mjs",
    "example": "node examples/build-example.mjs",
    "build:wasm": "emcmake cmake -S core -B core/build-wasm -G Ninja -DCMAKE_BUILD_TYPE=Release && cmake --build core/build-wasm"
  },
  "exports": {
    ".": "./src/index.js"
  },
  "files": [
    "src/"
  ],
  "license": "SEE LICENSE IN CONTRIBUTING.md"
}
