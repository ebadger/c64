diff --git a/src/dos.s b/src/dos.s
index 1ff9fc4..6815c86 100644
--- a/src/dos.s
+++ b/src/dos.s
@@ -2507,15 +2507,23 @@ dir_find_in_buf:
     adc #5
     tay
     ldx #0
 @cmp:
+    ; comodines CBM: '*' acepta el resto del nombre; '?' acepta un caracter
+    lda NAME_BUF,x
+    cmp #'*'
+    beq @matched
+    cmp #'?'
+    beq @advance
     lda DIR_BUF,y
     cmp NAME_BUF,x
     bne @next
+@advance:
     iny
     inx
     cpx #16
     bne @cmp
     ; coincide
+@matched:
     jsr dir_extract
     lda #$FF
     sta dir_found
